# =============================================================================
# CUDA INSTALLATION NOTES
# =============================================================================
# For GPU support, install PyTorch with CUDA from the official PyTorch website:
# https://pytorch.org/get-started/locally/
#
# Example CUDA installation commands:
# CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU only:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Check your CUDA version with: nvidia-smi
# This project REQUIRES GPU with at least 80GB VRAM for 27B models
# =============================================================================

# Core ML Libraries
# NOTE: Install torch with CUDA support from pytorch.org (see above)
# torch>=2.0.0  # Install separately with CUDA support
transformers>=4.35.0
accelerate>=0.20.0

# NOTE: We do NOT need torchvision, torchaudio, or other vision/audio libraries
# This is a text-only NLP project for prompt tuning

# LangChain and LangGraph
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-huggingface>=0.0.1
langgraph>=0.0.30

# Data Processing
numpy>=1.24.0

# NOTE: pandas is not required for this specific project
# Uncomment if you need data analysis features:
# pandas>=2.0.0

# Utilities
typing-extensions>=4.5.0
pydantic>=2.0.0

# Tokenization and Model Utilities
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.3.0

# Optional: Quantization support (currently not used but available)
bitsandbytes>=0.41.0

# Optional: Development and Visualization
# Uncomment if needed for analysis:
# jupyter>=1.0.0
# matplotlib>=3.7.0
# seaborn>=0.12.0

# System utilities
psutil>=5.9.0

# =============================================================================
# INSTALLATION INSTRUCTIONS
# =============================================================================
# 1. First install PyTorch with CUDA:
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# 2. Then install other requirements:
#    pip install -r requirements.txt
#
# 3. Verify GPU is available:
#    python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}')"
# =============================================================================

# NOTE: The following are NOT needed for this project:
# - torchvision (computer vision tasks)
# - torchaudio (audio processing)
# - opencv-python (image processing)
# - pillow (image handling)
# - scikit-learn (traditional ML)
# - tensorflow (different ML framework)